<div class = 'nav'>
  <%= link_to t('.edit'), edit_admin_test_path(@test) unless params[:action] == 'edit'%> 
  <%= link_to t('.del'), admin_test_path(@test), method: 'delete', data: {confirm: "Are you shure?"} %> 
  <%= link_to t('.list'), admin_tests_path %>
</div>
