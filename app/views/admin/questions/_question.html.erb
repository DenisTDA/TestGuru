<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('nav.show'), admin_question_path(question) %></td>
  <td><%= link_to t('nav.edit'), edit_admin_question_path(question) %></td>
  <td><%= link_to t('nav.del'), admin_question_path(question), method: "delete", data: {confirm: 'Are you sure?'} %></td>
</tr>
